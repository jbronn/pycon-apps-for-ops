# Debian-specific settings.
---
# Debian packages for setuptools/pip/virtualenv are extremeley
# out-of-date on Debian -- set packages to false to bootstrap
# with ez_setup / easy_install pip / pip install virtualenv.
python::ez_setup: true
